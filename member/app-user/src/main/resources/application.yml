server:
  port: 30001

# 日志级别（输出见 logback-spring.xml：控制台着色 + ./log/{应用名}/ 按天归档，保留 3 天）
logging:
  level:
    root: INFO
    com.xcz.member: DEBUG

spring:
  application:
    name: app-admin
  servlet:
    multipart:
      max-file-size: 20MB
      max-request-size: 120MB
  config:
    import:
      - optional:file:../.env[.properties]
      - optional:nacos:${spring.application.name}.yaml
  cloud:
    nacos:
      discovery:
        server-addr: ${NACOS_SERVER_ADDR:127.0.0.1:8848}
        username: ${NACOS_USERNAME:nacos}
        password: ${NACOS_PASSWORD}
      config:
        server-addr: ${NACOS_SERVER_ADDR:127.0.0.1:8848}
        namespace: ${NACOS_NAMESPACE:public}
        group: ${NACOS_GROUP:MINI_APP_MOBI}
        file-extension: yaml
        username: ${NACOS_USERNAME:nacos}
        password: ${NACOS_PASSWORD}

#nacos中配置
#spring:
#  # Dynamic-Datasource 多数据源配置
#  datasource:
#    dynamic:
#      # 默认数据源
#      primary: master
#      # 严格模式：false 表示匹配不到数据源时使用默认数据源
#      strict: false
#      # 数据源列表
#      datasource:
#        # 主数据源（写库）
#        master:
#          driver-class-name: com.mysql.cj.jdbc.Driver
#          url: jdbc:mysql://127.0.0.1:3306/min_admin?useUnicode=true&characterEncoding=utf8&zeroDateTimeBehavior=convertToNull&useSSL=true&serverTimezone=GMT%2B8
#          username: root
#          password: ${MYSQL_PASSWORD}
#          druid:
#            initial-size: 5
#            min-idle: 10
#            max-active: 20
#            max-wait: 60000
#            time-between-eviction-runs-millis: 60000
#            min-evictable-idle-time-millis: 300000
#            max-evictable-idle-time-millis: 900000
#            validation-query: SELECT 1
#            test-while-idle: true
#            test-on-borrow: false
#            test-on-return: false
#
#        # 从数据源（只读库）
#        slave:
#          enable: false
#
## MyBatis-Plus 配置
#mybatis-plus:
#  # 搜索指定包别名
#  type-aliases-package: com.xcz.member
#  # 配置 mapper 的扫描，找到所有的 mapper.xml 映射文件
#  mapper-locations: classpath*:**/mapper/**/*Mapper.xml
#  # 全局配置
#  global-config:
#    db-config:
#      # 主键类型（AUTO: 数据库 ID 自增）
#      id-type: AUTO
#      # 逻辑删除配置
#      logic-delete-field: delFlag
#      logic-delete-value: 1
#      logic-not-delete-value: 0
#  # 分页配置（注意：这里应该是 mybatis-plus.global-config 下的配置）
#  configuration:
#    # 是否开启自动驼峰命名规则映射
#    map-underscore-to-camel-case: true
#    # 日志输出（开发环境可开启）
#    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
#  page:
#    overflow: false
#    max-limit: 500
#
## Redis 配置
#redis:
#  enabled: true
#  single:
#    host: 127.0.0.1
#    port: 6379
#    password: ${REDIS_PASSWORD}
#
#log:
#  request:
#    enabled: true
#
#
#
## 安全配置
#security:
#  # JWT 配置
#  jwt:
#    # 密钥
#    secret: ${JWT_SECRET}
#    # 过期时间（单位：毫秒）7 天
#    expiration: 604800000
#  ignore:
#    urls:
#      - /system/auth/login
#  internal-service:
#    token: ${INTERNAL_SERVICE_TOKEN}
#    # 以下为默认值，一般不用改；/shop/sys/statistics 已在 /shop/sys/** 内
#    inbound-auth-applications:
#      - app-admin
#    protected-paths:
#      - /shop/sys/**
#      - /mobi/user/sys/**
#      - /mobi/role/**
#      - /ticket/sys/**
#      - /user-coupon/sys/**
#      - /points/sys/**
#      - /product/list
#      - /product/status

