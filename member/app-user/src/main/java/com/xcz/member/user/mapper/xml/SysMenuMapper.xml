<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xcz.member.user.mapper.SysMenuMapper">

    <resultMap id="SysMenuResult" type="com.xcz.member.user.domain.SysMenu">
        <id property="menuId" column="menu_id"/>
        <result property="menuName" column="menu_name"/>
        <result property="parentId" column="parent_id"/>
        <result property="orderNum" column="order_num"/>
        <result property="path" column="path"/>
        <result property="component" column="component"/>
        <result property="query" column="query"/>
        <result property="isFrame" column="is_frame"/>
        <result property="isCache" column="is_cache"/>
        <result property="menuType" column="menu_type"/>
        <result property="visible" column="visible"/>
        <result property="status" column="status"/>
        <result property="perms" column="perms"/>
        <result property="icon" column="icon"/>
        <result property="createBy" column="create_by"/>
        <result property="createTime" column="create_time"/>
        <result property="updateBy" column="update_by"/>
        <result property="updateTime" column="update_time"/>
        <result property="remark" column="remark"/>
    </resultMap>

    <delete id="deleteRoleMenu">
        DELETE FROM sys_role_menu
        WHERE menu_id IN
        <foreach collection="menuIds" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

    <select id="selectMenusByUserId" resultMap="SysMenuResult">
        SELECT DISTINCT m.* FROM sys_menu m
        LEFT JOIN sys_role_menu rm ON m.menu_id = rm.menu_id
        LEFT JOIN sys_user_role ur ON rm.role_id = ur.role_id
        WHERE ur.user_id = #{userId} AND m.status = '0'
        ORDER BY m.parent_id, m.order_num
    </select>

    <select id="selectMenusByRoleId" resultMap="SysMenuResult">
        SELECT m.* FROM sys_menu m
        LEFT JOIN sys_role_menu rm ON m.menu_id = rm.menu_id
        WHERE rm.role_id = #{roleId} AND m.status = '0'
        ORDER BY m.parent_id, m.order_num
    </select>
    <select id="selectAllMenus" resultMap="SysMenuResult">
        SELECT * FROM sys_menu
        WHERE del_flag = 0
        ORDER BY parent_id, order_num
    </select>

    <select id="selectPermissions" resultType="com.xcz.member.user.domain.vo.PermissionVo">
        SELECT
            sr.role_key roleKey,
            sm.perms permission
        FROM sys_role sr
        INNER JOIN sys_role_menu srm on sr.role_id = srm.role_id
        INNER JOIN sys_menu sm on srm.menu_id = sm.menu_id
        WHERE sr.status = 0 and sm.status = 0
    </select>

</mapper>
