<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xcz.member.customer.infrastructure.mapper.MobiUserCouponsMapper">

    <update id="markUsedIfUnused">
        UPDATE mobi_user_coupons
        SET status = #{usedStatus},
            used_time = #{usedTime}
        WHERE user_coupon_id = #{userCouponId}
          AND status = #{unusedStatus}
    </update>
</mapper>
