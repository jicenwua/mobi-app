<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xcz.member.customer.infrastructure.mapper.MobiShopUserMapper">

    <update id="update">
        UPDATE mobi_shop_user
        SET last_enter_time =
        <foreach collection="list" item="item" open="CASE" close="END">
            WHEN shop_id = #{item.shopId} THEN #{item.lastEnterTime}
        </foreach>
        WHERE user_id = #{userId}
        AND role = #{roleType}
        AND shop_id IN
        <foreach collection="list" item="item" open="(" separator="," close=")">
            #{item.shopId}
        </foreach>
    </update>

    <update id="batchUpdateLastEnterTime">
        UPDATE mobi_shop_user
        SET last_enter_time = CASE shop_id
        <foreach collection="list" item="item">
            WHEN #{item.shopId} THEN #{item.lastEnterTime}
        </foreach>
        END
        WHERE user_id = #{userId}
        AND shop_id IN
        <foreach collection="list" item="item" open="(" separator="," close=")">
            #{item.shopId}
        </foreach>
    </update>
</mapper>
